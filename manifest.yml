---
declared-services:
  twilio:
    label: Twilio
    plan: 'user-provided'
  slackbot_webhook:
    label: slackbot_webhook
    plan: 'user-provided'   
  speech_to_text:
    label: speech_to_text
    plan: free
applications:
- name: pickme-voice-agent 
  memory: 256M 
  command: node app.js
  buildpack: https://github.com/jthomas/nodejs-buildpack.git
  services:
  - twilio
  - speech_to_text
  - slackbot_webhook
